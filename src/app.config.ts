export class AppConfig  {

    /* remote service for currency exchange values */
    static currencyServiceAddress: string = 'https://api.exchangeratesapi.io/latest';

    /* API service port listening number */
    static listenPort: number = 3000;

}